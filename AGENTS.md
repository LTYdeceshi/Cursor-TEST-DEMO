# AGENTS.md

## Cursor Cloud specific instructions

This is an empty test/demo repository (`Cursor-TEST-DEMO`). There are no application services, dependencies, or build systems to configure.

### Available tooling

- **Python**: 3.12 (system)
- **Node.js**: 22.x (via nvm)
- **Git**: 2.43

### Notes

- The repo contains only a `README.md`. There are no `package.json`, `requirements.txt`, `Makefile`, `Dockerfile`, or other config files.
- No lint, test, or build commands exist. If application code is added in the future, update this file with the relevant commands.
- No services need to be started.
